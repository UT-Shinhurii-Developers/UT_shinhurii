<p><%= @resource.name %>さん、こんにちは!</p>

<p>リンクをクリックして、パスワードを再設定しましょう。</p>

<p><%= link_to '再設定', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>このメールに見覚えがなければ、無視してください。</p>
<p>あなたのパスワードは、リンクが押されない限り変更されません。</p>
